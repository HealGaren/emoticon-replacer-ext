name = "emoticon-replacer-config"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# `wrangler kv namespace create CONFIG_KV` 실행 후 출력된 id를 아래에 기입한다.
[[kv_namespaces]]
binding = "CONFIG_KV"
id = "aeb4f4d1e2ce4b4cb172f44f8f39e304"

# ADMIN_TOKEN은 secret으로 설정한다(평문으로 두지 말 것):
#   wrangler secret put ADMIN_TOKEN
